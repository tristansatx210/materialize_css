<% if obj.errors.any? %>
  <div class="container">
    <div class="row enroll">
      <div class="col s12 m12 l12">
        <div class="card-panel red lighten-2">
          <span class="white-text">
          Please fix 
          <%= pluralize(obj.errors.count, "error") %>
            <ul>
              <strong><% obj.errors.full_messages.each do |msg| %></strong>
                <li>
                  <%= msg %>
                  
                </li>
              <% end %>
            </ul>
            <% end %>
          </span>  
        </div>
      </div>
    </div>
  </div>